## feature
Server logs stored in SQLite (7-day retention) with admin panel Logs tab
All log.Printf output is captured to DB with auto-detected level (info/warn/error). Admin UI shows filterable, auto-refreshing log viewer.
