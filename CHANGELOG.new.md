## fix
D3 stall detector now rebuilds transport
Previously left tunnel wedged in Reconnecting state forever.

## improvement
Extension collapses discovered hosts to eTLD+1
Reduces domain list size on SPAs with many subdomains.
