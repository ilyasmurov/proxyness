# Go
dist/
*.exe
/cmd

# Node
client/node_modules/
client/dist/
client/dist-electron/
client/release/
client/resources/daemon-*
client/resources/helper-*
client/resources/seed_sites.json
server/admin-ui/node_modules/
server/admin-ui/dist/
server/internal/admin/static/
!server/internal/admin/static/.gitkeep

# TLS certs / SSH keys
cert.pem
key.pem
*.pem

# Superpowers
.superpowers/

# Changelog
CHANGELOG.new.md

# Local design previews / mockups — kept on disk for reference but not
# tracked in git. Includes assets/*.html (client, landing, extension,
# logo variants) and docs/*-preview.html (tray/settings previews).
assets/*.html
docs/*-preview.html

# OS
.DS_Store
Thumbs.db

# Extension
extension/build/
mobile/
# Added by code-review-graph
.code-review-graph/
